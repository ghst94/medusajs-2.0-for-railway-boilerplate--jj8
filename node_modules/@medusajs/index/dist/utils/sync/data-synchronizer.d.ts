import { IndexTypes, RemoteQueryFunction } from "@medusajs/framework/types";
export declare class DataSynchronizer {
    #private;
    constructor({ storageProvider, schemaObjectRepresentation, query, }: {
        storageProvider: IndexTypes.StorageProvider;
        schemaObjectRepresentation: IndexTypes.SchemaObjectRepresentation;
        query: RemoteQueryFunction;
    });
    sync({ entityName, pagination, ack, }: {
        entityName: string;
        pagination?: {
            cursor?: string;
            updated_at?: string | Date;
            limit?: number;
            batchSize?: number;
        };
        ack: (ack: {
            lastCursor: string | null;
            done?: boolean;
            err?: Error;
        }) => Promise<void>;
    }): Promise<{
        lastCursor: string;
        done?: boolean;
        err?: Error;
    } | undefined>;
}
//# sourceMappingURL=data-synchronizer.d.ts.map